{
  "name": "Kuching Bus Tracker Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24-bookworm",
  
  // Install Hugo, Go, and Protobuf CLI tools natively into the container features layer
  "features": {
    "ghcr.io/devcontainers/features/go:1": {},
    "ghcr.io/devcontainers/features/hugo:1": {
      "version": "0.161.1",
      "extended": true
    }
  },

  // Structured chain execution sequence to handle Linux packages, local Node modules, automated ESLint initializations, and initial data parsing automatically
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y protobuf-compiler libgtk2.0-0 libgtk-3-0 libgbm-dev libnotify-dev libnss3 libxss1 libasound2 libxtst6 xauth xvfb && [ ! -f package.json ] && npm init -y || true && npm install --save-dev wrangler cypress gtfs-realtime-bindings eslint @eslint/js globals eslint-plugin-cypress && ( [ ! -f eslint.config.js ] && echo 'const js = require(\"@eslint/js\");\nconst globals = require(\"globals\");\nconst cypressPlugin = require(\"eslint-plugin-cypress\");\n\nmodule.exports = [\n  js.configs.recommended,\n  {\n    files: [\"assets/js/**/*.js\"],\n    languageOptions: {\n      ecmaVersion: 2024,\n      sourceType: \"script\",\n      globals: {\n        ...globals.browser,\n        L: \"readonly\",\n        getDisplayRouteCode: \"readonly\",\n        calculateHaversineDistance: \"readonly\",\n        renderSelectedRouteLine: \"readonly\",\n        renderFilteredBusStops: \"readonly\",\n        startUserLocationTracking: \"readonly\",\n        stopUserLocationTracking: \"readonly\",\n        syncLiveBusTracker: \"readonly\",\n        injectDynamicCopyrightYear: \"readonly\"\n      }\n    },\n    rules: {\n      \"no-unused-vars\": [\"warn\", { \"vars\": \"all\", \"args\": \"none\" }],\n      \"no-undef\": \"error\",\n      \"no-console\": \"off\"\n    }\n  },\n  {\n    files: [\".devcontainer/scripts/*.js\"],\n    languageOptions: {\n      ecmaVersion: 2024,\n      sourceType: \"commonjs\",\n      globals: {\n        ...globals.node\n      }\n    }\n  },\n  {\n    files: [\"cypress/e2e/**/*.cy.js\", \"cypress/**/*.js\"],\n    plugins: {\n      cypress: cypressPlugin\n    },\n    languageOptions: {\n      ecmaVersion: 2024,\n      sourceType: \"script\",\n      globals: {\n        ...globals.browser,\n        ...cypressPlugin.configs.recommended.languageOptions.globals\n      }\n    },\n    rules: {\n      ...cypressPlugin.configs.recommended.rules,\n      \"cypress/no-unnecessary-waiting\": \"warn\",\n      \"no-undef\": \"error\",\n      \"no-unused-vars\": \"off\"\n    }\n  }\n];' > eslint.config.js || true ) && npx cypress install && bash .devcontainer/setup.sh",

  // Configure VS Code settings and pre-load your extensions
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "files.associations": {
          "*.proto": "protobuf"
        }
      },
      "extensions": [
        "dbaeumer.vscode-eslint",       // Pre-load real-time linting and style diagnostics directly in the IDE
        "drblury.protobuf-vsc",         // Advanced Protobuf schema support & formatting
        "anotak.protobuf-inspector",    // Live inline proto binary parsing layout
        "rangav.vscode-thunder-client", // GUI-based API client for checking data.gov.my payloads
        "bep.hugo-shortcodes",          // Autocomplete helper for Hugo shortcodes
        "sheinsight.cypress-io"         // Run and debug Cypress tests directly from VS Code UI
      ]
    }
  },

  // Forward local ports to your machine 
  "forwardPorts": [1313, 8788, 3000, 6080],

  // Ensure the container runs safely as a non-root user
  "remoteUser": "node"
}
